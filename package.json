{
  "name": "LearningCafe",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "testcafe": "^1.5.0",
    "testcafe-react-selectors": "^3.3.0"
  },
  "scripts": {
    "chrome": "ENV=dev testcafe chrome tests",
    "chrome:headless": "ENV=dev testcafe chrome:headless tests",
    "chrome:debug": "ENV=dev testcafe chrome tests --debug-on-fail --speed 0.7",
    "chrome:react": "ENV=dev testcafe chrome tests/react-test-case.js --debug-on-fail --speed 0.5",
    "firefox": "ENV=dev testcafe firefox tests",
    "firefox:debug": "ENV=dev testcafe firefox tests --debug-on-fail --speed 0.7"
  }
}
